//4

let hours =prompt("ingrese la cantidad de horas");
const  costhour= 3
if (parseInt(hours) >= 1) {
    let pay = ((hours) * (costhour))
    console.log("el costo total es d: $"+pay)
} else {
    if (parseInt(hours) <= 0) {
        console.log("el parqueadero no tiene costo ya que no se utilizo")
    } else {
        if (hours = String) {
            console.log("ingrese una opcion validad")
        } else 
            if (parseFloat(hours) >=0,1 || parseInt(hours) >= 1)
            pay = ((hours) * (costhour))
            console.log("el costo total es d: $"+pay)
        
    }
}